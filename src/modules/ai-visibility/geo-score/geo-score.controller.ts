import { Controller } from '@nestjs/common'

/**
 * GEO Score — auditoria + cálculo da nota de visibilidade em IA.
 * Placeholder do Dia 1: sem rotas ainda (o motor de scoring vem no Sprint 2).
 */
@Controller('ai-visibility/geo-score')
export class GeoScoreController {}
